---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: k3kconfigtemplates.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    kind: K3kConfigTemplate
    listKind: K3kConfigTemplateList
    plural: k3kconfigtemplates
    singular: k3kconfigtemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: K3kConfigTemplate is the Schema for the k3kconfigtemplates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: K3kConfigTemplateSpec defines the desired state of K3kConfigTemplate
            properties:
              agentConfig:
                description: |-
                  AgentConfig is the config/args for k3s running in an agent configuration.
                  One and only one of ServerConfig and AgentConfig must be provided. Will deploy
                  in agent mode if provided.
                properties:
                  serverArgs:
                    items:
                      type: string
                    type: array
                type: object
              serverConfig:
                description: |-
                  ServerConfig is the config/args for k3s running in a server configuration.
                  One and only one of ServerConfig and AgentConfig must be provided. Will deploy
                  in server mode if provided.
                properties:
                  serverArgs:
                    items:
                      type: string
                    type: array
                type: object
            type: object
          status:
            description: K3kConfigTemplateStatus defines the observed state of K3kConfigTemplate
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
