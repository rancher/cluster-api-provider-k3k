---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: k3kcontrolplanes.controlplane.cluster.x-k8s.io
spec:
  group: controlplane.cluster.x-k8s.io
  names:
    kind: K3kControlPlane
    listKind: K3kControlPlaneList
    plural: k3kcontrolplanes
    singular: k3kcontrolplane
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: K3kControlPlane is the Schema for the k3kcontrolplanes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: K3kControlPlaneSpec defines the desired state of K3kControlPlane
            properties:
              addons:
                description: Addons are the addons which will also be deployed on
                  the cluster.
                items:
                  properties:
                    secretNamespace:
                      type: string
                    secretRef:
                      type: string
                  type: object
                type: array
              clusterCIDR:
                description: ClusterCIDR is the CIDR for the cluster.
                type: string
              clusterDNS:
                description: ClusterDNS is the cluster DNS.
                type: string
              serviceCIDR:
                description: ServiceCIDR is the CIDR for the services.
                type: string
              tlsSANs:
                description: TLSSANs is the SANs for the TLS cert the cluster will
                  use.
                items:
                  type: string
                type: array
              version:
                description: Version is the k8s version of the k3k cluster.
                type: string
            required:
            - version
            type: object
          status:
            description: K3kControlPlaneStatus defines the observed state of K3kControlPlane
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
